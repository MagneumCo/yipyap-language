interface:
  display_name: "YipYap (Language)"
  short_description: "Language learning with YipYap"
  default_prompt: "Use $yipyap-language to report my current YipYap (Language) setup status."
policy:
  allow_implicit_invocation: true
